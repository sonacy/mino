{
  "name": "mino",
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@name/common,@name/server}",
    "build:web": "lerna run build --scope={@name/common,@name/web}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": []
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}